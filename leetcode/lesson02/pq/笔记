PQ:
优先队列

找前K大的数：
1、排序 n log(n)
2、

数据结构： 二叉堆(完全二叉树）---数组

array[0] 优先级最高


i
父节点： (i-1) / 2
左子节点： 2*i + 1
右子节点： 2*i + 2

插入：
swimUp O(logK)
swimDown O(log(k)) ---- 取出堆顶元素，堆底和堆顶更换，然后下沉。

优先队列初始化时间复杂度： o(n)
原因：


LC347 前K 高频率的元素
